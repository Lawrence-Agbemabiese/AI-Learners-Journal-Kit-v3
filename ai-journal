#!/usr/bin/env bash
# AI Journal CLI wrapper.

set -euo pipefail

JOURNAL_DIR="${AI_JOURNAL_DIR:-$HOME/AI-Journal}"
SCRIPTS_DIR="${AI_JOURNAL_SCRIPTS_DIR:-$JOURNAL_DIR/scripts}"
PYTHON_BIN="${PYTHON_BIN:-python3}"

exec "$PYTHON_BIN" "$SCRIPTS_DIR/journal_cli.py" "$@"
